# Implementation Plan: 已选安全诊断与检索设计

## Selected Design And Constraints

本计划配套[完整设计](../proposals/bounded-result-retrieval.md)，2026-09-20 用户已选定方向并要求文档交付。此文件是后续实施交接，不是运行时代码已完成的证明。实现时无需再次逐项确认已经选定的架构；真实接收方权限不能从架构授权推导。

2026-09-23 补充[首版实施契约](contracts.md)，工作包按该文档的状态、字段、提交点及 K01–K20 反例落地。内部 Python 组织由实施者决定。当前核对源码为 `06ab2d3`，原证据摘要标识历史快照，不能覆盖后续更正。

已选边界：旧结果仅安全元数据；新结果保存首次回执的同一安全投影；敏感日志不进模型；隔离测试支持丰富诊断；摘要绑定可信引用；检索不授予外发或交付权限；固定产物外发复用 D9/D7；页大小、累计限额与窗口由真实实验选择。

## Source Revision And Drift Check

设计基线：`84d22889228e629b5cd6f146591aca750408ea77`。
证据集合 SHA-256：`f08de167e205c1d9ddc586cda367fe452fdf8c84d79217d79d4024c6b29e92d6`，算法与文件清单见 [context.md](../context.md)。
设计检查时有并行修改 `tests/test_unified_runtime.py`，未纳入证据或更改。

实施前按 AGENTS.md 对齐工作区、本地提交与远端，重新检查本方案涉及源码及配置。如其他会话已修复 F20/F21/F22，读实际变更与测试，不重复修复。新增状态必须是 typed event＋精确 expected version；不可用文档中的伪 API 名假定生产已接线。

## Affected Components

| 现有组件 | 接入责任 |
| --- | --- |
| `src/koawa_agent_v2/verification/tools.py`、runner/sandbox | 测试输出适配、安全环境证据、固定释放契约 |
| `src/koawa_agent_v2/mcp/connection_manager.py` | 来源绑定、MCP 回执投影、发出参数的接收方约束 |
| `src/koawa_agent_v2/execution/loop.py`、model adapter | 所有回执/恢复入口、最终完整请求门、模型服务变更校验 |
| `src/koawa_agent_v2/execution/worker.py`、recovery | seed 与上下文身份对齐、投影引用重放、异常终态 |
| `src/koawa_agent_v2/ledger/store.py`、event store | 执行结果与投影状态分离；JSON 发布事件与 CAS |
| `src/koawa_agent_v2/runtime/session.py`、压缩与摘要 | 安全历史投影、可信引用、受限滚动摘要 |
| policy/approval/executor | 产物、接收方、负载、策略和 MCP 身份的动作绑定 |
| 新增必要组件（包完成时再建） | 投影存储/适配器、派生索引、搜索读取、外发适配器；不建未来空模块 |

上述映射是职责，不要求一次改变所有文件。实现者应沿现有边界复用，避免同时引入新服务、向量库和全局标签传播系统。

## Ordered Work Packages

### WP-A：恢复与压缩基线核验

复现同线程首轮、后续新 turn、失败回合、恢复、连续压缩。F20 验收不能只测“有历史前缀”：按 f20-correction.md 的六项 seed 与后续压缩条件检查替换后的索引/版本映射，并对比已知成功运行。空 seed/非空 loop 只作为额外防御性测试，不作为已确认根因。比较规范化后的上下文身份、顺序与来源映射，而不是原始/脱敏字符串混比。

压缩 intended/compacted 的前置校验、崩溃窗口与重放要分别验证。F21 终态处理只能防止线程卡住，不能证明已有执行流一致。未通过前，可做独立组件，但不能对外启用依赖错误恢复链的检索。

### WP-B：数据契约、入口门与完整请求门

建立本地受信任 ProjectionPolicy，输入包括来源、实际隔离证明、模型接收方、字段释放规则和当前策略版本。unknown 默认正文不可见；受限状态也须有释放契约。外部字符串不能创建 trusted 标签。

入口覆盖普通 tool result、恢复回执、session preload、摘要模型、检索工具、错误与异常输出；最终 provider adapter 再验证请求中每项 provenance，防止漏接入口。工具 schema/描述同样受配置来源和大小约束，但可见不代表有指令权威。

定义最终 request 计量接口，包含真实序列化输入和 provider 已知开销/预留；任何压缩开关与提前返回不影响该检查。先用离线合成请求建立测量正确性，再实测估算误差。验收：所有通往 provider 的实际路径使用门禁，未知模型计量配置不能静默零估算。

### WP-C：安全诊断环境与适配器

为测试建立仅允许代码/依赖/合成输入的独立工作区，核验挂载、镜像、环境和禁网络，保护路径解析与复制时的身份一致性。现有 host runner 和整仓库挂载默认保持受限输出身份。

对 MCP 按服务/工具/schema/绑定建立输出适配器；未知正文不参与摘要、embedding 或搜索预览。详细诊断可包含哪些堆栈与值取决于已证明的输入可见性。真实敏感测试只运行维护者定义的固定释放契约，防止模型借真假查询提取秘密。

验收以合成秘密覆盖 env、文件、镜像、链接、网络和日志编码；不是只断言启动参数含 network none。容器不可用时明确禁用详细路径，不自动切换宿主后继续共享正文。

2026-09-21 已选细化：profile 初始清单展开为固定文件集合，依赖扩展只在已授权范围内自动进行；代码/脚本/敏感输入只读，每次运行专属临时区可写，禁止宿主工作区写回、网络和共享敏感缓存。所有派生文件继承受限属性，默认不长期保留；导出经容器外可信适配器，不能直接打开容器网络。具体边界见完整设计“敏感测试容器的具体约定”。

WP-C 增补验收：目录新文件与链接不能绕过清单；准备后输入变化被检测或使用固定快照；临时数据库/编译缓存可正常使用但不改宿主；敏感值藏在文件名、截图、压缩包、崩溃文件中均不能进入模型；失败清理后的暂存区不可复用；安全重测不继承敏感缓存；审批不能扩大成整个容器的通用能力；候选代码变化不能绕过敏感验证规则，跨 turn/恢复不能重置披露预算。没有具体释放契约时只返回允许的固定不可用状态。

2026-09-22 已确认依赖准备：项目配置/锁文件/测试与 CI 提供候选清单，无敏感输入的隔离试跑发现缺项，授权范围内自动补齐，再固定为 profile。新增敏感资源另行授权；配置文本和缺依赖报错本身不授予权限。依赖安装在无敏感输入的受控准备环境，敏感运行禁联网。补充验收：动态依赖缺项、恶意安装脚本、生产数据库请求、补齐预算耗尽均不扩大权限或回退整仓库挂载。

2026-09-23 已确认候选准入：合成环境完成阶段性调试后固定候选，绑定代码/测试/依赖/运行配置/输入版本/profile；维护者预授权范围内自动准入，超出范围或任意新代码接触高敏感数据且对模型返回结果时，默认本地审查固定候选。批准后任何绑定项变化使原批准不适用。结果释放仍须独立满足 profile 契约，候选准入不代替披露授权。

增补验收：批准后替换代码/依赖/数据或测试规则被拒；相同路径和 generation 不掩盖内容变化；新候选、换 turn、恢复不重置累计披露限制；已知结果依法复用、unknown 不盲重跑；代码路径白名单或模型审查意见不能独立授予无限敏感验证能力。数值阈值仍由真实实验确定。

2026-09-23 已确认自动化边界：日常修补以合成环境详细诊断为主，敏感测试主要承担受控验证。确实禁止披露结论时使用独立的结果不可提供状态，停止依赖该结果的自动修补并交人工；信息不足造成任务无法自动完成是明确接受的取舍。

增补验收：结果不可提供不映射为失败/通过，不触发补丁修改或探测式重测；独立安全工作可继续，依赖部分明确待人工验证；整体交付不得声称全部验证通过。人工交接不把敏感正文或隐藏结论经模型传递，提供固定候选与获准证据；人工补充说明仍受模型入口策略约束。执行账本状态与披露状态分离。

2026-09-23 已确认测试快照：每次验证绑定代码、测试、依赖、环境/profile 与输入版本；独立测试工作区身份接入现有验证证据。验收覆盖 H2 测试通过后工作区变为 H3、测试脚本/依赖变化、构建产物与源输入不匹配、测试试图写候选代码等场景。历史通过不能直接用于已变化候选；生成/编译步骤须显式绑定输入与产物。

2026-09-23 缓存已委托定稿：受控依赖缓存和安全构建缓存按输入身份及当前权限只读复用；敏感运行产物首版不跨运行复用。共享区只有受控发布器可写，测试写入私有临时区；半成品不可命中，损坏/撤销视为未命中并隔离，不回退敏感容器联网安装。缓存回收保护活跃引用，缓存命中不等于测试通过。具体身份组成与反例见完整设计“测试缓存复用”。

### WP-D：JSON 投影发布与留存

落实完整设计的发布提交点。拟定领域事件族：result.projection-published.v1、result.projection-unavailable.v1、result.projection-revoked.v1、result.projection-expired.v1；命名可按仓库规范细化，含义不能混为工具成功/失败。正文是不可变 JSON，发布事件记录正文引用和安全元数据；写入使用 expected stream version。

执行终态先有可信记录；投影失败不得重新调用工具。模型首次回执必须来自已发布的同一投影；未发布则返回安全不可用状态，不能临时回显私有文本。幂等发布键绑定 execution_id＋投影契约版本；重试不能写出同身份不同内容。

注入故障：工具已完成/正文未写、正文已写/事件 CAS 冲突、事件已写/索引未更新、撤销已写/清理未完成。验收：无越权可见孤儿、无自动重执行、可辨别正文不可用、清理有状态。

### WP-E：元数据搜索与投影读取

当前线程和工作区作用域从 execution context 取。索引由已发布事件增量重建，正文不可索引时不提取关键词。旧事件仅允许已审核的身份/状态字段；现有脱敏记录不自动获准全文。

游标绑定结果或查询、水位、策略范围和过期，固定排序确保翻页不重复混淆版本。返回包含 complete/truncated/availability，索引水位不足不能冒充全量无结果。配额同时约束响应、总读取、扫描工作量和存储；计数不可通过重复页、新 turn 或恢复重置。

验收：伪造 ref/cursor、权限撤销、换模型接收方、跨线程、索引缺失/损坏均不触发原始事件正文回退。

### WP-F：摘要引用、保护组与成本控制

摘要引用只绑定当前权限内真实记录，保留来源与适用基线；无效引用不能落入可信证据区。历史压缩不遗漏当前约束，不破坏未完成 call/result。长期摘要可滚动合并，已完成旧用户回合不无限豁免，歧义约束保留或明确容量不足。

稳定前缀减少无关变化，近期消息与检索内容靠后追加。不保留越权/陈旧授权内容来提高缓存，不把缓存失效当错误。补压缩后检索、旧摘要再次压缩后搜索、正文过期等测试。

### WP-G：固定产物外发

实现 export_artifact_ref 与 destination_ref 的可信解析，构造固定字节负载再绑定 D9 action。受限数据的模型改写不是可追踪来源证明，默认不能以“模型自己写的报告”为由外发。

检查实际发送的 URL、账户、参数、标题、文件名和内容；凭据只由授权通道注入，未持久化。执行前与实际发送处绑定同一快照；重定向或绑定变化重新判定。盘点现有远程搜索、MCP、网络测试和 shell 旁路，没有纳管的路径在受保护模式关闭。

发送超时保留 D7 uncertain；批准不会重置账本。验收同时覆盖批准后产物变化、跨账户目标、MCP refresh、断线、重复调用与恢复。

### WP-H：真实长任务与上线参数报告

完成下面的实验矩阵后生成参数选择报告，写明候选参数、测量结果、置信限制与最终选值。报告记录配置摘要和代码版本，可供后续复测。安全不变量失败的候选直接淘汰，不用成本收益抵消。

## Compatibility And Migration

旧库不改写历史事件。建立安全派生索引；恢复入口重新投影未知正文为不可用说明，并保持 tool call/result 协议闭合。新事件用版本化 schema 和兼容读取器；旧二进制不能在不识别新可见性门的情况下继续发送模型请求。

新投影正文到期不等于执行证据作废；必要最小事实留存遵从原运行恢复契约。过期索引不能指向被复用的 ID。备份与恢复必须携带撤销水位，不能复活已撤销投影。

## Tactical Protections During Migration

保留 D7 claim/账本、D9 动作摘要/批准、现有工具身份、generation 和文件基线检查。旧敏感正文不可通过“先做一个小功能”开放；检索不可用时明确返回不可用，不降级读取原始日志。F20/F21 修复独立验收，不以新索引绕过。

## Tests And Security Validation

详细矩阵见完整设计 Validation Plan。安全样本均用合成秘密。每包运行受影响测试；完成实现切片时按仓库规则在 v2 目录设置 PYTHONPATH=src，运行：

```powershell
$env:PYTHONPATH = "src"
python -m unittest discover -s tests -v
```

记录 Python 版本、跳过原因和真实 Docker/provider 覆盖范围；skipped 不能算成实际隔离测试通过。文档交付阶段只做文档结构、链接、JSON、证据哈希与变更范围校验，本次不声明上述实现测试已经运行。

## Performance And Resource Benchmarks

| 待定参数/决策 | 工作负载与量测 | 选择规则 |
| --- | --- | --- |
| 单页字节/token、搜索候选数 | 短状态、长堆栈、中文、MCP JSON、分割关键诊断 | 保留诊断完整度并限制单次请求开销 |
| 累计页数/读取量/查询工作量 | 重复页、跨 turn、kill/resume、无效搜索循环 | 正常长任务可完成，恶意或无效循环有有限上界 |
| 软/硬窗口、输出预留、摘要触发 | 稳定工具目录与大目录、长用户输入、多模块任务 | 最终不超 provider 限额；任务质量与总成本共同比较 |
| 投影/线程存储与保留期限 | 多轮会话、过期后复查、事件重建 | 满足复查用途，增长与清理可控；正文到期可解释 |
| 摘要合并节奏与缓存 | 同模型同任务比较不同合并策略 | 用 provider 报告的 cached/input tokens 和总费用衡量 |
| 详细诊断契约覆盖 | 可安全复现与必须真实数据的失败 | 分开统计修复成功与诊断不足，不能混成总成功 |

先跑离线可靠性/容量基线，再跑相同任务集合的三种模式：无检索、元数据检索、安全投影检索。记录任务完成率、修复正确性、定位关键原因比例、错误历史证据使用次数、轮数/重试、输入输出与缓存 token、总费用、检索及工具完成 p95、峰值 RSS、磁盘增长、索引重建和清理时间。

测量请求容量、用户可见工具调用次数与 provider 请求轮数时分别命名，不混为“轮”。现有 run10 的 82.3% 缓存命中只是一条既有冒烟记录，不当成本设计的收益基准或承诺。

所有运行模式都需有限配置值，但本设计不拍数字。测试实施时从可验证的 provider 限额与测试资源约束生成候选，再记录参数来源；正式启用前必须有参数报告，不能把示例数值带进默认配置。

## Rollout And Rollback

先离线契约与故障测试，再受限来源灰度，再真实长任务。每一阶段都保留首返模型入口和最终容量检查。可独立关闭详细诊断、检索、外发；关闭正文发布后仅返回安全状态。不能回滚到会重新开放旧敏感正文的旧路径。

投影策略出错时先撤销可见性、再清理，监测模型请求与发送日志的安全元数据。已经对外发送不可撤回，不宣称策略修复能逆转既往披露。

## Design Detail And Implementation Discretion

用户委托设计方判断细致程度。文档应达到“实施者能据此写验收测试，无需自行选择安全语义”的粒度；不提前规定所有 Python 类、函数或内部目录。以下内容是每个工作包开工前的就绪检查，不表示当前全部细则已经补齐：

- 输入与输出：来源、可信/不可信身份、字段含义、允许值、缺失/未知的处理、谁可见；通过/失败/未完成/不可披露不能混用。
- 权限与执行位置：谁作决定、依据哪个策略版本、在哪个实际读写/发送边界执行；不可由模型文本或对象引用自行授予权力。
- 状态与顺序：成功提交点、typed event 与 expected version、崩溃窗口、幂等和重试条件、撤销及清理，明确工具执行与诊断可用性的独立状态。
- 绑定与失效：候选、输入、产物、接收方和环境如何识别，哪些变化使缓存/批准/验证结果失效；不能只写“校验一致”。
- 失败行为：预算不足、缺资源、权限撤销、存储失败、unknown 和证据不足时允许什么、禁止什么、向模型/人分别反馈什么。
- 验收反例：具体输入、动作、预期状态与可观察结果；至少覆盖正常路径、拒绝路径和恢复路径，不能只写“确保安全”。

实现者可以选择内部类名、模块拆分、辅助函数和不改变契约的数据结构。模型可见协议、持久化 schema、错误语义、授权范围、默认放行/拒绝及恢复行为属于契约；需在相应工作包中明确后实现，不能以“实现细节”为由另作选择。参数保留为待实测项，但作用域、计量口径、超限行为和不可绕过条件须先确定。

每包实施前给出简短的契约映射：设计条款 → 现有/新增执行边界 → 对应测试，并列出未确定项；已确认规则不重复请示。技术歧义可在既定权限内保守收口，遇到需要扩大权限或改变用户已选行为才回到设计讨论。具体 profile 权限实例或运行参数缺失时，只阻止相关能力启用，不冒充全套已就绪。用户已明确“下一部分”默认确认前一方案；有过修订的讨论以最终接受版本为准。

2026-09-23 状态整理：回执原则、敏感产物清理、策略变更复核和泄漏后保守处置均已确认。异常处置验收包括暂停受影响的模型调用、禁止外部模型清洗秘密、停止服务端旧历史续接、禁止自动读取受影响产物、仅凭安全信息重建；不承诺无损回填，也不自动回滚覆盖用户改动。清理验收覆盖成功/失败/取消/崩溃、资源归属核验、证据未落盘、清理失败隔离与不盲重跑。

首版实施契约已补齐通用权限表、回执语义、状态转移与崩溃行为、身份失效矩阵及验收反例。剩余是填写真实 profile 权限实例、将契约映射到代码和测试、以实测确定参数；不是重新选择架构。需要真实资源授权时才请求权限。

## Acceptance Criteria

完成条件同时满足：全部既定安全矩阵通过；真实首轮/同线程后续 turn/恢复/压缩/交付链可运行；检索到的证据适用范围可核验；正文与权限撤销无旁路；D7 uncertain 没有盲目重发；真实任务测量报告给出有限参数；全量回归及实际依赖验证有记录。

尚未完成的包、依赖不可用和拒绝诊断的场景必须显式交付状态。不能仅凭单元测试、文档中的图或一个长任务成功宣称整个安全能力完成。

## Open Decisions

架构不再待裁决。实现者需补齐实际 provider/账户与接收方配置、允许测试输入和已审查 profile、实验预算及量测阈值。没有这些权限事实时可以推进离线实现与合成测试，不能对真实敏感数据或新接收方做自主授权。
