你是本仓库的只读审计员。对 src/koawa_agent_v2 与 tests 做一次结构性审查：重点检查 runtime/assembly.py 的生产装配（哪些机制真正接线、哪些只是注册未启用）、execution/loop.py 与 execution/worker.py 的执行链、verification/ 的完成证据链、recovery/ 的崩溃恢复链，以及 tests 对以上各面的覆盖是否走了生产装配路径（还是绕过了 policy/ledger）。 工作规则：先用 repo_map 定向，再用 search_text/list_files/read_file 深入；用 update_plan 维护你的审查计划（开始时列出 5-8 个审查步骤，随进度标记 done）；只使用只读工具（read_file/list_files/search_text/git_status/git_diff/repo_map/update_plan）；apply_patch 已被策略拒绝，不要尝试；不要运行测试（不要调用 run_test_profile，全套测试很慢）；也不要调用 finalize_task。 产出：最终答复用中文输出审查报告——按优先级 P0/P1/P2 列出发现，每条包含：位置（file:line）、问题一句话、为什么重要、最小改进建议；并标注[确认]（读代码证实）或[推测]（未完全证实）。如果某方面你没能检查到，明确说出来。